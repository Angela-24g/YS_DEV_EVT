# Ping 통신 테스트

## 1. 사용배경
통신 테스트를 통과한 차량 단말기 위치에 해당하는 링크의 평균 시간을 맵으로 표현하고 싶다.
(1) 통신테스트

## 2. 사용기술
- ProcessBuilder
- BufferedReader

## 3. 설계 / 구조
ping을 이용하여 통신 테스트를 하고, 데이터를 읽고자 함.
그 후, restTemplate을 이용하여 처리하고자 하는 서버에 요청을 보내는 방법

## 4. 코드
```java
    // 1. 리눅스 ping 테스트를 하므로, 리눅스에 접근 및 데이터 읽어오는 예제
    ProcessBuilder pb = new ProcessBuilder(command);
    pb.redirectErrorStream(true);
    Process process = pb.start();

    BufferedReader reader = new BufferedReader(new InputStreamReader(process.getInputStream()));
    // 데이터를 버퍼로 읽기 때문에, 데이터 형식의 제한이나 분열 등을 걱정하지 않아도 됨(동시에 많은 양의 데이터를 받아들이기에 적합한 구조)

    PingResult result = PingResult.of(target);
    if (isWindows) {
          command.addAll(Arrays.asList(
              "ping", "-n", "4",           // count
              "-l", "256",                 // size
              "-w", "12000",               // timeout (ms)
              target
          ));
        } 
    // 패킷 결과 패턴
    Pattern pattern =  isWindows ? Pattern.compile("Sent = (\\d+), Received = (\\d+), Lost = \\d+ \\((\\d+)% loss\\)");
    
    String line;  // 여기서 굳이 line 변수를 넣는 이유는, 파일을 읽어오면서 한 번 더 호출이 되면 다음 줄을 읽어버린다. 디버깅과 데이터 안정성을 위함
    while ((line = reader.readLine()) != null){
    Matcher m1 = pattern.matcher(line);
    }
    
    
    
    // 2. 
```

## 5. 참고
https://soonmin.tistory.com/108 (processbuilder)
https://velog.io/@roycewon/BufferedReader%EB%A5%BC-%EC%95%8C%EA%B3%A0-%EC%93%B0%EC%9E%90 (bufferedbuilder)